# github-toolbox
GitHub 関連の便利ツール

# command 配下
## get_collaborators
- 全リポジトリのユーザ・権限一覧を作成、ファイルをExportする

# メモ

```bash
pip install -r requirements.txt

pip freeze > requirements.txt
```

```bash
python -m command.hogehoge
```
